apiVersion: v1
kind: ConfigMap
metadata:
  name: eros-template-configmap
data:
  config.yaml: |
    # Database configuration.
    database:
      logger:
        level:  "all"
        stdout: true
      default:
        link:  "mysql:root:change_me@tcp(127.0.0.1:3306)/gf?loc=Local&parseTime=true"
        debug: true

    # Logger configuration.
    logger:
      level : "all"
      stdout: true
