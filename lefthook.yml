pre-commit:
  jobs:
    - name: ci-reminder
      run: echo "Repository verification runs in GitHub Actions after push."

pre-push:
  jobs:
    - name: ci-reminder
      run: echo "Repository verification runs in GitHub Actions after this push. Watch CI GBaseAdmin; main branch also runs Deploy GBaseAdmin."
